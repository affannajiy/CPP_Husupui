#include <iostream> /*iostream header file for func input and output operations (ionamnip, cmath, algorithm, string)*/
//std (standard), c (char), out (output), endl (endline)// 
int main() {
	std::cout << "I like pizza!" << std::endl;
	std::cout << "It's really good!" << std::endl;
	return 0;
}